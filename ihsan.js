useFetch("geocode",options)
useFetch("geocodeBuffer",options)

const options= {
    param1: "sfdgj",
    param2: "doıfodas",
    param3: "ksdfjgsd"
}

const generator = {
    geocode : (options) => {
        return `${process.env.REACT_ENV_IVIR_ZIVIR}/ilce/mahalle?param1=${param1}?param2=${param2}?param3=${param3}`
    }
}

useFetch()

switch (options) {
    case value:
        
        break;

    default:
        break;
}